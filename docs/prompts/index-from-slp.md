# 当日プロンプト: slp から追加インデックス

本番中、Claude Code / 作業用 LLM に次を投げる。

---

以下は slp の出力です。Rows examined / Rows sent 比が悪いクエリを特定し、  
追加すべき複合インデックスを提案してください。

制約:

- 比が高い順に最大 5 本。1 本ごとに「クエリ要約 / 比 / 提案 INDEX / 理由」
- カバリングインデックスになる可能性も検討する（SELECT 列をキーに含められるか）
- 既存インデックスと重複するものは提案しない
- 最後に、人間が mysql で打てる `CREATE INDEX ...` だけを列挙する（存在確認の `SELECT COUNT(*)` も添える）
- MySQL 8 前提。`CREATE INDEX IF NOT EXISTS` は使わない
- ここから DB を変更しない。適用手順は docs/00300-measure.md の「インデックス」
- テーブル定義に無い列は作らない

テーブル定義も併せて渡します。

<slp の出力とスキーマを貼る>

---
